{
  "name": "moniterror",
  "version": "1.0.0",
  "description": "Local RAM & port usage dashboard — see what's eating memory, stop it, and see what's holding a port.",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.client.json",
    "start": "pnpm build && node dist/server.js",
    "dev": "pnpm build && node dist/server.js",
    "lint": "oxlint ."
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "oxlint": "^0.15.9",
    "typescript": "^5.7.2"
  }
}
